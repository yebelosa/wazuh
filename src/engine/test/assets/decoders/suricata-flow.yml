name: decoder/suricata-flow/0

sources:
  - decoder/suricata/0

metadata:
  description: suricata-flow

check:
  - ~json.event_type: flow

normalize:
  - map:
      - event.type: +a_append/connection

---
name: decoder/cisco-ios-grandchild/0

sources:
  # TODO: Should has a list of parents
  # - cisco-ios-sec-login
  - decoder/cisco-ios-sec/0

metadata:
  description: Cisco IOS last child, add common fields to cisco-ios event
  version:
    - Cisco IOS System, 15 SY
    - Cisco ASA version 9.10

check:
  - service.type: cisco


normalize:
  - map:
      - network.type: +s_ip_version/$source.ip
      - related.ip: +a_append/$source.ip/$destination.ip
      - tags: +a_append/cisco-ios
